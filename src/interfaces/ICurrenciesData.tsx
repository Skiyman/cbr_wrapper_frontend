export interface ICurrencyData {
    currency_str_code: string,
    currency_name: string,
    currency_denomination: number,
    currency_rate: number,
    currency_iso_code: number,
    currency_unit_rate: number

}